/**
 * General configuration constants for the application.
 *
 * @module constant
 */
export const APP = {
  NAME: 'TrackPlay',
  DEFAULT_LANGUAGE: 'es',
  SUPPORTED_LANGUAGES: ['es', 'en'] as const,
}
